#ifndef WEAPON_HPP
#define WEAPON_HPP
#include "bullet.hpp"
#include "weaponinterface.hpp"
#include<vector>
class Weapon{
	private:
	public:
		Weapon();
		virtual ~Weapon();
		virtual void UseWeapon(WeaponInterface* wi);
};
#endif
