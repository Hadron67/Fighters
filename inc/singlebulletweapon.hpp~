#ifndef SINGLEBULLETWEAPON_HPP
#define SINGLEBULLETWEAPON_HPP
#include "weapon.hpp"
class SingleBulletWeapon{
	private:
		double bullet_t,bullet_v;
	public:
		SingleBulletWeapon(double t,double v);
		virtual ~SingleBulletWeapon();
		virtual void UseWeapon(WeaponInterface* wf);
};
#endif
